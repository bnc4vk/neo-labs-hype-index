# Notes

## Outstanding items
- `ENV.md` now uses placeholders; replace `DATABASE_URL` and `SEARCH_API_KEY` with real values in local `.env` and GitHub Secrets.
- GitHub Actions secrets required: `DATABASE_URL`, `SEARCH_API_KEY`.

## Contradictions
- None found between SPEC.md, ARCHITECTURE.md, DATA_MODEL.md, INGESTION_SOURCES.md, and ENV.md.
