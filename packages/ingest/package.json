{
  "name": "@neolabs/ingest",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "ingest": "node --import tsx src/index.ts refresh",
    "ingest:bootstrap": "node --import tsx src/index.ts bootstrap",
    "ingest:refresh": "node --import tsx src/index.ts refresh",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@neolabs/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4",
    "vitest": "^2.1.3"
  }
}
