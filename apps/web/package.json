{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"console.log('Open apps/web/index.html in a browser or serve this folder with a static server.')\"",
    "build": "node -e \"['index.html','app.js','config.js','style.css'].forEach(f=>require('fs').accessSync(f)); console.log('static web build ok')\"",
    "lint": "node -e \"console.log('static web lint ok')\""
  }
}
